/*
Write a function that takes an array of scores and the highest possible score
and returns a sorted array of scores in less than O(n log n) time.
*/

function sortScores(arrayOfScores, highestPossibleScore) {
  
}

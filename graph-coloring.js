/*
Write a function to find a graph coloring for an undirected graph with maximum degree D using at most D+1 colors.
*/

class GraphNode {
  constructor(label) {
    this.label = label;
    this.neighbors = new Set();
    this.color = null;
  }
}

function findColoring(graph, colors) {

}

/*
Write a function to check if a binary tree is "superbalanced", meaning the difference between the depth of any two leaf nodes is no greater than one.

  Idea 1: Using breadth-first search, find the least-depth leaf node, then determine whether any branch has nodes beyond the next-lowest level.
*/

function isSuperBalanced(binaryTree) {

}
